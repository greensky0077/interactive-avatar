{
  "version": 2,
  "builds": [
    { "src": "server/server.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "server/server.js" }
  ],
  "buildCommand": "npm --prefix react-avatar-app ci && npm --prefix react-avatar-app run build && npm --prefix server ci"
}


