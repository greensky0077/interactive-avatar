{
  "version": 2,
  "builds": [
    { "src": "api/index.mjs", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/index.mjs" }
  ],
  "buildCommand": "npm ci && npm --prefix react-avatar-app ci && npm --prefix react-avatar-app run build && npm --prefix server ci"
}


